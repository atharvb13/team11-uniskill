VITE_API_BASE_URL=http://localhost:4000
# Required for email confirmation redirect: parse hash tokens and clean the URL on /email-confirmed
# Also required for Google OAuth sign-in via Supabase.
VITE_SUPABASE_URL=https://oraqkfrtjqapupyplrce.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_key
# Match FRONTEND_ORIGIN in uniskill-backend/.env (default http://localhost:5173).
# Add http://localhost:5173/auth/callback to Supabase Authentication redirect URLs.
